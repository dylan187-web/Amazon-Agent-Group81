---
name: amazon-agent-group81
description: 亚马逊卖家通用选品 Agent。用户说「对 <种子词> 跑一遍」或点名单个阶段（选品 / 利润 / Listing / 广告）时使用。按 S1 选品 → S2 利润核算 → S3 Listing → S4 广告 顺序执行，数据来自卖家精灵 MCP，每阶段写一个产出文件。
---

# Amazon Agent Group81 · 路由入口

## 输入参数

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| 种子词 | 是 | — | 英文搜索词或类目词，如 `bamboo drawer organizer`。不接受品牌词 / IP 词，遇到就停下来提示用户换词 |
| 站点 | 否 | `US` | 卖家精灵站点代码 |
| 采购价 | 否 | 空 | 美元 / 件。填了，S2 就用它 |
| 头程 | 否 | 空 | 美元 / 件。填了，S2 就用它 |
| 从哪个阶段开始 | 否 | `S1` | 从 S2 以后开始时，必须有上一阶段的产出文件（本次运行目录或 `samples/`） |

## 运行目录

每次运行建一个目录：`runs/<YYYY-MM-DD>_<种子词，空格换成->/`

```
runs/2026-09-12_bamboo-drawer-organizer/
├── s1_选品.md
├── s2_利润.md
├── s3_listing.md
├── s4_广告.md
└── raw/            卖家精灵原始返回，文件名 <工具名>_<关键参数>.json
```

## 执行顺序

对每个阶段依次执行：

1. 读 `stages/<阶段>/prompt.md`，按其中的步骤做。
2. 读上一阶段产出（S1 除外）。
3. 调卖家精灵 MCP 前，先看 `raw/` 里有没有同名缓存，有就直接读。
4. 写出本阶段产出文件，**对照 `contracts/交接内容.md` 检查必需字段**，缺了就补查或标「未查到」，不编。
5. 在产出文件末尾写：本阶段卖家精灵调用次数（新调用 / 读缓存）。

| 阶段 | 提示词 | 读 | 写 |
|---|---|---|---|
| S1 选品 | `stages/s1-选品/prompt.md` | 输入参数 | `s1_选品.md` |
| S2 利润核算 | `stages/s2-利润/prompt.md` | `s1_选品.md` | `s2_利润.md` |
| S3 Listing | `stages/s3-listing/prompt.md` | `s2_利润.md`（+ `s1_选品.md`） | `s3_listing.md` |
| S4 广告 | `stages/s4-广告/prompt.md` | `s3_listing.md` | `s4_广告.md` |

**停止条件**：S2 没有任何「值得做」的候选时，停在 S2，告诉用户原因，建议换种子词。不要硬往下跑。

## 收尾

四个阶段跑完，在对话里给用户 5 行总结：
1. 依据的在涨词
2. 选中的候选品和它来自哪个词
3. 利润结论（估算 / 用户成本）
4. Listing 标题
5. 广告首月预算怎么分 + 本次卖家精灵总调用次数
