# S4 广告交接 · 二十。（2026-09-13）

负责人：二十。

开发工具：Codex

交付策略版本：s4-v1.0

状态：负责人审核通过，提交 PR 供 Dylan 合并。

## 交付范围

- S4 提示词：相关性筛选 → 缓存与竞价补查 → 匹配方式 → 预算 → 否定预案 → 输出校验。
- Python 辅助校验、Markdown 渲染、版本快照和差异对比；广告策略判断仍由模型执行。本次不进一步封装广告执行脚本。
- 对齐课程 S3 虚构样例的 S4 draft / 报告，以及 28 项离线测试。
- 保持 S3→S4 交接：针对同一候选，投放原词只能来自 S3 关键词清单；支持 Highlights、长描述等位置。

## 已确认策略与修改入口

策略配置统一在 [S4 prompt.md](../stages/s4-广告/prompt.md) 的“策略配置区”：初始预算 60/30/10；空组归零、非空组归一化；相关性筛词；按购买意图选匹配；缺竞价写“未查到”；否定词写范围与依据。校验器不写死上述广告打法。

后续修改策略时递增 strategy_version，先保留上一轮快照，再用同一 S3 输入和缓存重跑。review_iteration.py 可输出选词、匹配、预算、竞价、否词前后差异。改 S3 输入或接口才重新跑全链路。

## 验证

- 命令：python -m unittest discover -s stages/s4-广告 -p "test_*.py" -v；28 项通过。
- 虚构样例：samples/s3_listing.md → S4，结构校验通过。
- 本地实际试跑：以 bamboo drawer organizer、US、无采购/头程输入，按原基线 fe90d09 执行 S1→S4；S3 现有检查器 28 项、整链来源检查 24 项通过。
- 卖家精灵实际调用：S1 新10/缓存0，S2 新0/缓存1，S3 新4/缓存5，S4 新1/缓存3；合计新15/缓存9。
- S4 实际试跑保留4个投放词、暂缓2个。广泛组为空，预算调整为精准66.67%/词组33.33%/广泛0%；未提供总预算，不写金额。
- 原始返回、完整报告、调用账本、问题清单和 v1 快照保留在本机 runs/2026-09-13_bamboo-drawer-organizer/；本 PR 只提交实现、虚构样例与交接，不上传本轮原始数据。

## 留给集成评审的问题

- S1 默认关键词/商品查询曾返回不相关结果，本轮通过已有兜底及显式商品匹配参数补救；候选又集中在同一父体，需要 S1 负责人复核独立性和销量口径。
- S3 评论为混合变体样本，产品规格仍待确认；词池较少。上游问题已记录，本次没有修改其他负责人的实现。
- S4 有1个词补查后仍没有竞价，保留“未查到”；缺少真实广告数据，不能以结构通过声称业务效果已验证。
- 交接后由 Dylan 合并 PR；比赛表单、视频等整组提交仍按 docs/提交清单.md 执行。
