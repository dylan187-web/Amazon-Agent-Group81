---
type: guide
status: 草稿
scope: Amazon Agent Group81（跨境 Agent 大课 · 黑客松小组作业）
created: 2026-09-12
updated: 2026-09-12
---

# 协作开发要领 · 多人 + 多 AI 并行开发

> 适用：小组成员分头用 Codex / Claude Code 开发各自模块，最后合并进这个公开仓库，作为黑客松作业。
> 本文先于 PRD 写，讲的是**怎么分工、怎么合并不打架**；做什么由 `PRD.md` 定。
> 标注：【已核实】= 查过仓库或 GitHub 确认过；【建议】= 经验判断，可以改。
>
> ⚠️ **本文写于 SPEC 之前。与 `SPEC.md` 冲突时以 SPEC 为准**：形态定为 Skill（不是 Python 命令行），模块目录是 `stages/`，接口是 `contracts/交接内容.md`，阶段认领在 README 表格里登记。

---

## 0. 一句话

**先定接口，再分模块；每人只动自己的目录，按分支提 PR 合并；进度靠 GitHub Issue 和 PR 看，不靠大家一起改同一份 md。**

并行开发最容易翻车的不是写代码，而是**最后合并时发现各写各的对不上**。下面每一条都是为了避免这件事。

---

## 1. 仓库现状【已核实 2026-09-12】

| 项 | 现状 | 影响 |
|---|---|---|
| 可见性 | **PUBLIC（公开）** | 推上去的东西全世界可见，而且会留在提交历史里，删文件也删不掉 |
| License | **没有** | 严格说别人没有使用和贡献的授权，开源仓库要补 |
| 协作者 | 只有 `dylan187-web` | 组员现在推不了代码，要么加成协作者，要么 fork 后提 PR |
| main 分支保护 | **没开** | 任何有权限的人都能直接推 main，容易把别人的代码冲掉 |
| `.gitignore` | **没有** | 密钥、数据、`.DS_Store`（本机上一级目录已经有了）会被误提交 |
| 提交记录 | 只有 1 个 `Initial commit`，包含 7 份模板和 `.gitattributes` | 干净，现在改结构成本最低 |

---

## 2. 现有模板合不合适

总体来说**骨架能用，但它们是给「一个人 + 多个 AI 轮流接力」设计的**（来自 Obsidian Vault 的 `00-多agent合作开发项目模板`），**不是给「多个人同时开发」设计的**。主要有三个问题：

1. **共享状态文件会冲突**：`任务清单`、`SOURCE_OF_TRUTH` 要求每个人领任务和收工时都改同一张表。几个人同时改就是 git 冲突，还可能出现两个人领了同一个任务。
2. **写死了你个人 Vault 的上下文**：比如「Vault 根目录 `AGENTS.md`」「问 Dylan」「跑 `kickoff` / `handoff` 技能」，但仓库里并没有 `.agents/skills/`。另外 Obsidian 的 `[[双链]]` 在 GitHub 上不会渲染成链接。
3. **相对路径是错的**：模板在子目录里写 `../AGENTS.md`、`../SOURCE_OF_TRUTH.md`，放到仓库根目录就指到仓库外面去了。

逐个看：

| 文件 | 结论 | 怎么改【建议】 |
|---|---|---|
| `AGENTS_模板.md` → `AGENTS.md` | ✅ 保留，要改写 | 删掉 Vault、个人口吻和技能路径；加上「只改自己模块目录」「不改接口约定」「提 PR 前跑测试」这几条（见第 5 节） |
| `CLAUDE_模板.md` → `CLAUDE.md` | ✅ 直接用 | `@AGENTS.md` 这行是对的，只把正文里的 `../AGENTS.md` 改成 `AGENTS.md` |
| `PRD_模板.md` → `PRD.md` | ✅ 保留，要补两节 | 补「模块划分与负责人」「模块之间的接口约定」，这两节是并行开发的关键 |
| `任务清单_模板.md` | ⚠️ 改用法 | **领任务改成在 GitHub Issue 里 assign 自己**，谁在做一眼就能看到，也不会冲突。md 版如果要留，只由负责集成的人更新 |
| `SOURCE_OF_TRUTH_模板.md` | ⚠️ 改用法 | **只由负责集成的人（你）更新**，组员不改 |
| `交接_模板.md` | ✅ 保留 | 文件名改成 `99_交接/YYYY-MM-DD_姓名_任务号.md`，每人一份，天然不冲突 |
| `README.md` | ❌ 要重写 | 现在写的是「模板怎么用」，公开仓库首页应该写：这是什么 Agent、怎么跑、怎么参与开发 |
| `.gitattributes` | ✅ 保留 | 统一换行符，Mac 和 Windows 混用时有用 |
| 缺的 | 补 | `LICENSE`（建议 MIT）、`.gitignore`、`.env.example`、`CONTRIBUTING.md`（也可以并进 README） |

模板改成正式文件之后，`*_模板.md` 要不要删、还是挪到 `templates/` 里，**由你定**。

---

## 3. 开源前必须先做的（安全与隐私）

公开仓库的东西**推上去就收不回**，所以这一节比写代码优先。

1. **密钥绝不进仓库**。卖家精灵、Claude、OpenAI 等 key 一律放 `.env`，`.gitignore` 把 `.env` 排除，仓库只放 `.env.example`（只写变量名、不写值）。万一误推，**马上作废那个 key**，删文件没用，历史里还在。
   - 脚本要读 key 时一律读环境变量，每个组员用自己的 key。
2. **真实业务数据不进仓库**：店铺名、自家 ASIN 业绩、销量、利润、后台或 ERP 导出、个人资料都不放。测试用**假数据或脱敏样例**，放 `samples/`。
3. **大文件不进仓库**：ABA 解压后有 13.1 GB，GitHub 单个文件超过 100 MB 直接拒收。数据放在各自本机，仓库里只写「数据从哪来、放哪个路径」。
4. **侵权词 / IP 词不进代码、样例和 prompt。**
5. **补 LICENSE**，否则严格来说不算开源。

---

## 4. 并行开发要领

### 4.1 先定接口，再分模块（最重要）

- PRD 里给每个模块写清：**输入是什么、输出是什么**。用一个 JSON 样例说明，把字段名、类型、单位（美元还是澳元、天还是周）写死。
- 接口放在 `contracts/`（格式说明）和 `samples/`（每个接口一份样例文件）。
- **每个人先对着样例开发，不等别人的代码**。比如 A 做数据读取、B 做补货计算，B 一开始直接读 `samples/` 里的假数据就能开工。
- **接口一旦定下，改之前要在群里说一声，由你拍板**，并且通知所有用到它的人。私自改接口是合并失败的头号原因。

### 4.2 按目录划分归属

```
modules/<模块名>/     每个模块一个目录，一个负责人；自带 README 和测试
shared/              公共代码（配置读取、日志、数据结构），改动要你 review
contracts/ samples/  接口约定和样例，改动要你拍板
docs/                PRD、协作要领等
99_交接/             每人每次收工一份
```

（目录名等 PRD 定了再落地。上面是【建议】。）

原则：**一个 PR 只动自己模块的目录**。要动 `shared/` 或 `contracts/`，单独提一个小 PR。

### 4.3 先搭一条能从头跑到尾的「空骨架」

分工之前，先由一个人（建议你）把**整条流程搭成最小可跑版**：每个模块都是个假函数，只返回样例数据，但 `一条命令` 能从输入跑到输出。
然后大家各自把自己那段假函数换成真实现。这样**任何时候 main 分支都能跑**，最后不会出现合并大爆炸。

### 4.4 分支与合并流程

1. 每个任务一个 Issue，领任务 = 在 Issue 上 assign 自己。
2. 从最新的 main 拉分支：`分支名 = 任务号-简短描述`，比如 `T3-restock-calc`。
3. **小步提交、早提 PR**。一个 PR 最好一两天就能合进去，别憋一个大的到最后。
4. PR 描述写：做了什么、怎么验证、用的是 Codex 还是 Claude Code、关联哪个 Issue（写 `Closes #3`）。
5. 合并前至少一个人看过；**main 开分支保护，不允许直接推**。
6. 用 squash merge（一个 PR 在 main 上只留一条记录，历史干净）。
7. 每天开工前把 main 合进自己分支，冲突早发现早处理。

### 4.5 统一技术栈，第一天就定

语言和版本、依赖管理工具、代码格式化工具、跑测试的命令，**分工前定死并写进 `AGENTS.md`**。
不统一的后果：依赖锁文件互相冲突；AI 按各自习惯把整个文件重新格式化，一个小改动变成几百行 diff，根本没法 review。
【建议】用 Python，你现有的工作台脚本（`1_筛词.py`、`sp_call.py`）就是 Python。

### 4.6 集成节奏

- 定一个**代码冻结时间**，比如截止前 N 小时。之后只修 bug，不加功能。
- 冻结后由你负责集成：拉最新 main，从头跑一遍 demo，录屏或截图留证据。
- 截止时间〔待确认〕。

---

## 5. Codex 和 Claude Code 混用的注意事项

| 要点 | 做法 |
|---|---|
| 规则只写一份 | Codex 读 `AGENTS.md`，Claude Code 读 `CLAUDE.md`，而 `CLAUDE.md` 通过 `@AGENTS.md` 导入同一份规则。**所有规则只改 `AGENTS.md`** |
| 限定 AI 的改动范围 | `AGENTS.md` 写明：只改当前任务对应的模块目录；不改 `contracts/`、`shared/`，也不改别人的模块；不重新格式化无关文件 |
| 进度写进文件 | AI 会话记忆互不相通，也不跨人。任务要求写在 Issue 里，收工写 `99_交接/`，下一个 AI 从文件接上 |
| 提交前自检 | `AGENTS.md` 写明：提 PR 前必须跑通测试命令，并且确认没有提交 `.env` 或数据文件 |
| 不让 AI 自己推 main 或合 PR | AI 只负责在分支上改代码，提交和合并由人来点 |
| 提示词和 Agent 行为规则 | 如果 Agent 本身要调 LLM，prompt 文件放进各自模块目录并纳入版本管理，别只留在某个人的对话里 |

---

## 6. 写 PRD 之前要先定的事（等你和组里定）

1. **Agent 做什么**：是不是就做方向清单里的 D01 补货哨兵？
   - 反方考虑：补货哨兵要用你自己店铺的真实销量和库存，但仓库是公开的，组员也没有你的数据。如果选它，就要**先做一份脱敏样例数据**，否则组员没法开发和测试。也可以选一个用公开数据（比如 ABA、卖家精灵）就能跑的方向，协作会简单很多。
2. **截止时间、提交形式**：课程对黑客松作业要求是 demo、仓库链接还是演示视频？〔待确认〕
3. **几个人、各自的 GitHub 账号**：决定是加协作者还是走 fork。
4. **技术栈**（见 4.5）。
5. **外部服务的钱谁出**：卖家精灵、LLM 调用都按量计费，每人用自己的 key 还是共用？
6. **License**：建议 MIT，限制最少，方便组员和其他人合并。

---

## 7. 顺带提醒

- macOS 上如果以后要用 launchd 定时跑这个 Agent，仓库别放在 `~/Documents` 下，否则可能因隐私权限被静默挂起。手动跑不受影响。

---

## 版本

- v0.1 · 2026-09-12 · Claude Code 首稿：检查仓库现状与模板适配度，写出并行开发要领，待 Dylan 审。
- v0.2 · 2026-09-12 · 公开前清理：删去指向个人本地路径和个人资料的描述（原第 3 节第 1、2 条的具体路径与资料名、第 7 节两条个人提醒），开头加「与 SPEC 冲突以 SPEC 为准」。
