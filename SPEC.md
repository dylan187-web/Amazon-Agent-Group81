---
type: SPEC
status: 草稿（待 Dylan 审）
scope: Amazon Agent Group81（跨境 Agent 大课 · 黑客松小组作业）
created: 2026-09-12
updated: 2026-09-12
---

# SPEC · 亚马逊卖家通用选品 Agent（轻量版）

> 黑客松作业，6 小时内交演示视频。本文件**只定阶段、交接内容和红线**，每个阶段具体怎么做由负责人按自己的业务视角决定（铺货、精品都行）。
> 协作细则见 [`协作开发要领.md`](协作开发要领.md)。

## 已定决策（2026-09-12 与 Dylan 逐问确定）

| # | 问题 | 决定 |
|---|---|---|
| 1 | 用户 | 通用亚马逊卖家，铺货、精品都适用 |
| 2 | 选品方法 | 传统路子：热门搜索词 → 看趋势 → 找相关竞品 → 从竞品发现新需求，加利润核算 |
| 3 | 写多细 | 轻量：只定阶段、交接内容和红线，其余由负责人自定 |
| 4 | 形态 | Skill（提示词 + 少量 Python 脚本），Claude Code / Codex 都能跑 |
| 5 | 站点 | 默认美国站，站点是参数 |
| 6 | 利润口径 | 默认用卖家精灵估算；用户填了采购价、头程就用用户的 |
| 7 | 时间 / 人手 | 6 小时，4–5 人并行 |
| 8 | 链路范围 | 选品（简化）+ 利润 + Listing + 广告，四段都做，都轻量；「热门词趋势」和「竞品与新需求」合并为一个阶段 S1 选品，由一人负责 |
| 9 | 交接约定 | 阶段之间只定「交接内容」最低要求，其余自由 |

---

## 1. Problem Statement（问题陈述）

亚马逊卖家（铺货或精品）选品时，要在热门搜索词、趋势、竞品、利润之间来回切工具、手工拼数，慢，而且结论难追溯。选完品之后写 Listing、开广告，又要重新查一遍词，前后断层。

现成的全流程提示词方案（如 MIT 协议的 [`buluslan/bulus-amazon-skill`](https://github.com/buluslan/bulus-amazon-skill)，13 个场景）步骤多、烧额度——作者自己也说一口气跑全流程很烧 Token 和 MCP 额度。对只想快速判断「这个方向值不值得做、做的话怎么上架怎么开广告」的卖家来说太重。

## 2. Proposed Solution（方案描述）

一个 Skill：在 Claude Code 或 Codex 里说一句「对 `<种子词或类目>` 跑一遍」，按 4 个阶段往下走，**每个阶段写一个产出文件给下一阶段用**。

| 阶段 | 回答的问题 | 可用的卖家精灵 MCP 工具（负责人自选） |
|---|---|---|
| S1 选品（有趋势、可以选的品） | 哪些热门词在涨？这些词下卖得好的竞品是谁？从竞品的卖点、差评、变体、价格带，能看出什么没被满足的需求？ | `aba_research_weekly` / `aba_research_monthly`、`aba_research_trend`、`keyword_research`、`product_research`、`asin_detail`、`review`、`traffic_keyword` |
| S2 利润核算 | 候选品按估算或用户成本，能不能赚钱？ | `product_research` 的毛利 / FBA 费字段 + 用户输入 |
| S3 Listing | 给选中的候选写一版美国站 Listing（标题、五点、后台词），用的词要能说出来源 | `traffic_keyword`、`keyword_miner` |
| S4 广告 | 新品首月广告怎么开：投哪些词、什么匹配方式、预算大致怎么分 | 读 S3 的词清单；需要时用 `keyword_research` 查竞价 |

**分工**：一阶段一负责人，4 个阶段正好 4 人。骨架、交接约定、样例、合并、全链路跑通和录视频由 Dylan 负责（有第 5 人时可分出汇总报告 / 视频）。

### 唯一要统一的：交接内容（最低要求）

只要求下面这些字段必须有，格式、写法、分析方法随意。正式版放在 `contracts/`。

| 交接 | 必须包含 |
|---|---|
| S1 → S2 | ① 依据的在涨词，每个带：搜索量或排名、趋势方向、数据周期；② 候选竞品 ASIN 清单，每个带：来自哪个词、价格、销量估算、发现的需求点 |
| S2 → S3 | 每个候选的利润估算（写明是「估算」还是「用户成本」）+「值得做 / 观望 / 放弃」一句话理由；S3 只取「值得做」的第一个 |
| S3 → S4 | Listing 文本 + 用到的关键词清单（每个词带来源） |
| S4 输出 | 广告方案，只用 S3 词清单里的词 |

**并行开发的做法**：每个阶段先对着 `samples/` 里的样例输入开发，不等上游做完。

**铺货和精品的视角差异**放在各阶段提示词里自己处理（比如精品看差异化需求，铺货看快速动销），不在交接内容里区分。

## 3. Technical Constraints（技术约束）

- **时间**（官方规则：提交窗口为周六 20:00 至**周日 14:00**，周日 14:00 开始第一轮评选；以下为北京时间，时间表是建议）：
  - ✅ 已完成：骨架、交接约定、参考运行推到 main
  - 现在 → 11:30：各阶段并行开发
  - 11:30：代码冻结，合并
  - 12:00：全链路跑通一次，结果留存
  - 12:30：录屏、剪辑（≤5 分钟，≤200 MB，mp4，电脑录屏）
  - 13:15：封面（16:9 png）、作品名称、简介
  - 13:30：组长提交（不卡点，大文件上传要时间）
- **形态**：Skill，以 Markdown 提示词为主，脚本用 Python 3。Claude Code 放进 skills 路径即可；Codex 在 `AGENTS.md` 里写「读 `SKILL.md` 并照做」。
- **数据源**：只用卖家精灵 MCP，每人在自己的客户端配自己的 key，**仓库里不放任何 key**。卖家精灵的返回缓存到 `runs/`，避免重复扣费。
- **可复现**：Skill 每次跑结果会有差异，视频以 T+4:30 那次留存结果为主线。
- **公开仓库红线**：不放真实店铺数据（店铺名、自家 ASIN 业绩、利润、后台导出）、不放大文件、不用侵权词 / IP 词。
- **协作**：
  - 一阶段一目录一负责人，PR 只改自己的目录
  - `contracts/` 和 `SKILL.md` 只由 Dylan 改，要改先在群里说
  - 按分支提 PR，由 Dylan 合并

## 4. Non-goals（明确不做的事）

- 不绑定铺货或精品任何一种模式。
- 不接货源平台（1688 等），采购价由用户手填。
- 不对 Seller Central 做任何写操作：不自动上传 Listing、不自动建广告，只输出方案。
- 不做广告复盘（没有真实广告数据）、不做图片生成。
- 不做网页界面，不做澳洲站演示，不用本地 ABA 原始包。
- 不追求每次输出一致，只要求满足交接内容。

## 5. Success Criteria（成功标准）

1. **跑得通**：用一个演示种子词，从 S1 跑到 S4，4 份产出文件都生成，且满足交接内容最低要求。
2. **链路连着**：S1 的每个候选都能追到一个在涨的词；S3 用的是 S2 选出的候选；S4 的词都能在 S3 的词清单里找到。
3. **可追溯**：每个数字标来源工具和统计周期；每个候选都有利润估算和「做 / 观望 / 放弃」理由。
4. **两边都能跑**：至少一名 Codex 组员用同一仓库复跑成功。
5. **按时**：11:30 全部合进 main，周日 13:30 前由组长提交。视频 ≤5 分钟，展示：一句话触发 → 一个词一路追到一个候选品的利润结论 → 它的 Listing → 它的广告方案。
6. **仓库干净**：没有 key、真实业务数据、大文件。
7. **兜底**：到 11:30 如果某阶段没合进来，用 `runs/2026-09-12_bamboo-drawer-organizer/` 的参考运行结果补上，不硬凑、不空着。

## 6. 开放问题（按默认推进，不阻塞开工）

- [x] 视频要求：≤5 分钟 mp4、≤200 MB，电脑录屏不许手机拍；封面 16:9 png（官方规则，已核实）。提交清单见 `docs/提交清单.md`。
- [x] 演示种子词：沿用 `bamboo drawer organizer`（已真实跑通）。
- [x] 分工：S1 张也、花无缺；S2 Snow；S3 Dylan；S4 二十。；辅助 黄建雄、Y.
- [ ] 五个评分维度和权重：官方在组长群发布，〔待 Dylan 转发〕。
- [ ] Codex 是否原生支持 skills〔待核实〕；不影响方案，`AGENTS.md` 的写法不依赖它。

## 版本

- v0.1 · 2026-09-12 · Claude Code 与 Dylan 逐问梳理后首稿。修正记录：用户由「铺货卖家」改为「通用卖家」；选品简化为「热门词 → 趋势 → 竞品 → 新需求 + 利润」；各阶段不写死，只定交接内容。
- v0.2 · 2026-09-12 · Dylan 定：原 S1 搜索词与趋势、S2 竞品与新需求合并为 **S1 选品（有趋势、可以选的品）**，一人负责；阶段由 5 个改为 4 个（S1 选品 / S2 利润 / S3 Listing / S4 广告），交接表、成功标准、兜底条款同步改编号。原 S1→S2 交接的「在涨词」字段并入 S1 产出，保证候选品能追到词。
- v0.3 · 2026-09-13 · 按官方黑客松规则改时间表：由「T+ 相对时间」改为北京时间，截止周日 14:00；补视频、封面格式；开放问题更新。兜底条款改为用参考运行补位。
